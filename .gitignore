# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets — 절대 커밋 금지
.env
.env.local
.env.*.local
!.env.example

# 도구 캐시
.ruff_cache/
.pytest_cache/
htmlcov/
.coverage

prompts/output/

# Claude Code — 머신별 로컬 설정만 제외
.claude/settings.local.json
.claude/memory/

# SQLite 작업 이력 DB (컨테이너 볼륨)
data/*.db

# fastembed 임베딩 모델 캐시 (컨테이너 볼륨)
data/.fastembed/

# vault (개인 Obsidian 노트) — 로컬 전용, git 미추적 (.env 와 동일 취급)
# 워커가 런타임에 생성/인덱싱. 디스크엔 존재하되 git/origin 에는 올리지 않음.
vault/

logs/
