apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vllm-model-cache
  namespace: devops-pipeline
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 30Gi   # 모델 크기에 따라 조정 (7B ≈ 14GB, 13B ≈ 26GB)
  storageClassName: fast-ssd   # GPU 노드에 연결된 스토리지 클래스로 교체
